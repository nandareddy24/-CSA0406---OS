#include <unistd.h>

int main() {
    char buffer[100];
    int n;

    write(1,"Enter text: ",12);

    n=read(0,buffer,100);

    write(1,"You entered: ",13);
    write(1,buffer,n);

    return 0;
}
